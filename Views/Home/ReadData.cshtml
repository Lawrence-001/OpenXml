@{
    ViewData["Title"] = "Home Page";
}
<div class="row col-6 container-fluid justify-content-center">
    <form asp-action="ReadData">
        <div class="card">
            <div class="card-body">
            </div>
            <div class="card-footer">
                <button type="submit" asp-action="ReadData" asp-controller="Home"> Click Me</button>
            </div>
        </div>
    </form>

</div>
